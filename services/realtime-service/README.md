# realtime-service

Socket.io : sync parties, presence, chat live, push temps reel

Port : `5005`. Voir `docs/modules/realtime-service.md` pour la responsabilité, le périmètre fonctionnel, les
entités, l'API, les événements et les dépendances de ce service.

```bash
pnpm --filter realtime-service run dev
pnpm --filter realtime-service run test
```
